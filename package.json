{
  "name": "product-management-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:full": "concurrently \"pnpm --dir server dev\" \"pnpm --dir client dev\"",
    "dev:client": "pnpm --dir client dev",
    "dev:server": "pnpm --dir server dev",
    "build": "pnpm --dir client build",
    "setup": "pnpm --dir server setup"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}